namespace backend;

public class test
{
    /*
    var rentalPointStart = await _rentalPointRepository.GetByNameAsync(request.Name)                   
                           ?? throw new NotFoundException(nameof(User), nameof(User.Username), request.Username);
        
    var rentalPointEnd = await _rentalPointRepository.GetByNameAsync(request.Name)                   
                         ?? throw new NotFoundException(nameof(User), nameof(User.Username), request.Username);

    var car = await _carRepository.GetByModelAsync(request.Model)
              ?? throw new NotFoundException(nameof(User), nameof(User.Username), request.Username);

    
    if (!await car.ExistsByRentalPointId(rentalPoint.Id))
    {
        dana wypozyczalnia nie ma takiego auta   
    }

    if (!car.Available)
    {
        dane auto nie jest aktualnie dostepne
    }
            
    var rental = new Rental
    {
        ReviewId = request.ReviewId,
        UserId = userId,
        ReportStatus = ReportStatus.New
    };
    _rentalRepository.Add(rental);

    car.RentalPointEndId = rentalPointEnd;
    car.Available = false;

            
    await _unitOfWork.SaveAsync();
    */
}